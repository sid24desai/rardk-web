import { GithubEventRepository } from './github-event-repository';

describe('GithubEventRepository', () => {
  it('should create an instance', () => {
    expect(new GithubEventRepository()).toBeTruthy();
  });
});
