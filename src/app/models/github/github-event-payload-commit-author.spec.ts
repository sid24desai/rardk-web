import { GithubEventPayloadCommitAuthor } from './github-event-payload-commit-author';

describe('GithubEventPayloadCommitAuthor', () => {
  it('should create an instance', () => {
    expect(new GithubEventPayloadCommitAuthor()).toBeTruthy();
  });
});
