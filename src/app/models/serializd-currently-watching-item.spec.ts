import { SerializdCurrentlyWatchingItem } from './serializd-currently-watching-item';

describe('SerializdCurrentlyWatchingItem', () => {
  it('should create an instance', () => {
    expect(new SerializdCurrentlyWatchingItem()).toBeTruthy();
  });
});
